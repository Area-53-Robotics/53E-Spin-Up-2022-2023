#include "main.h"

// Misc helper functions
void printData() {
  //while (true) {
    // Print to terminal
    //std::cout << launcherMotor.get_position() << std::endl;
    // Print to brain
    //std::string position = std::to_string(launcherMotor.get_actual_velocity());
    //std::string power = std::to_string(70 - launcherEncoder.get_value()/10*1000);
    // Print to controller
    //controller.set_text(1, 0, position);
    //pros::delay(10); // We need a delay after each screen print
    //controller.set_text(0, 0, power);
    //pros::delay(10);
  //}
}
